// step 1: you can import packages in here, then export the packages
//  import foo from 'foo'
//  export { foo }

// step 2: when you need the package, you can use it like this:
//  import { foo } from 'relativeUrl/npm'

const foo = 'foo'
export {
  foo
}
